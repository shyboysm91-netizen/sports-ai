import { NextResponse } from "next/server";
import { listNews } from "../../lib/news-db";
export const dynamic="force-dynamic"; const SITE=process.env.NEXT_PUBLIC_SITE_URL||"https://장군분석.kr";
function x(v:string){return v.replace(/[<>&'\"]/g,c=>({"<":"&lt;",">":"&gt;","&":"&amp;","'":"&apos;",'"':"&quot;"})[c]||c);}
export async function GET(){let articles:Awaited<ReturnType<typeof listNews>>=[];try{articles=await listNews({status:"published",limit:50});}catch{}const items=articles.map(a=>{const link=`${SITE}/news/${a.slug}`;return `<item><title>${x(a.title)}</title><link>${x(link)}</link><guid isPermaLink="true">${x(link)}</guid><description>${x(a.summary)}</description><category>${x(a.category)}</category><pubDate>${new Date(a.publishedAt).toUTCString()}</pubDate></item>`}).join("");return new NextResponse(`<?xml version="1.0" encoding="UTF-8"?><rss version="2.0"><channel><title>장군분석 스포츠 뉴스</title><link>${SITE}/news</link><description>KBO, MLB, NPB, 축구, NBA 주요 뉴스와 데이터 해설</description><language>ko-KR</language><lastBuildDate>${new Date().toUTCString()}</lastBuildDate>${items}</channel></rss>`,{headers:{"Content-Type":"application/rss+xml; charset=utf-8","Cache-Control":"public, s-maxage=900, stale-while-revalidate=3600"}});}
