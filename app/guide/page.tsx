import type {Metadata} from "next";
import Link from "next/link";
import {EXISTING_GUIDES,GUIDE_ARTICLES} from "../lib/guides";
export const metadata:Metadata={title:"야구 분석 가이드",description:"KBO, MLB, NPB의 선발투수, 불펜, 타격 지표와 배당 기준을 쉽게 설명합니다.",alternates:{canonical:"/guide"}};
export default function Page(){const guides=[...EXISTING_GUIDES,...GUIDE_ARTICLES];return <main className="min-h-screen bg-slate-950 px-5 py-12 text-white"><div className="mx-auto max-w-5xl"><Link href="/" className="text-sm font-black text-blue-400">← 장군 AI 홈</Link><p className="mt-8 text-sm font-black text-blue-400">BASEBALL DATA GUIDE</p><h1 className="mt-2 text-4xl font-black">야구 분석을 쉽게 이해하는 방법</h1><p className="mt-4 max-w-3xl leading-7 text-slate-400">경기 결과를 단정하는 글이 아니라 장군 AI 화면에 표시되는 기록의 의미, 출처와 한계를 설명합니다.</p><div className="mt-10 grid gap-4 md:grid-cols-2">{guides.map(g=><Link key={g.slug} href={`/guide/${g.slug}`} className="rounded-2xl border border-slate-800 bg-slate-900 p-6 transition hover:border-blue-600"><h2 className="text-xl font-black">{g.title}</h2><p className="mt-3 leading-6 text-slate-400">{g.description}</p><p className="mt-4 text-sm font-black text-blue-400">읽어보기 →</p></Link>)}</div></div></main>}
