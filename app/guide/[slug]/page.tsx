import type {Metadata} from "next";
import Link from "next/link";
import {notFound} from "next/navigation";
import {GUIDE_ARTICLES} from "../../lib/guides";
export function generateStaticParams(){return GUIDE_ARTICLES.map(x=>({slug:x.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const a=GUIDE_ARTICLES.find(x=>x.slug===slug);return a?{title:a.title,description:a.description,alternates:{canonical:`/guide/${a.slug}`}}:{}}
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const a=GUIDE_ARTICLES.find(x=>x.slug===slug);if(!a)notFound();return <main className="min-h-screen bg-slate-950 px-5 py-12 text-white"><article className="mx-auto max-w-3xl"><div className="flex gap-4 text-sm font-black text-blue-400"><Link href="/">홈</Link><Link href="/guide">가이드 목록</Link></div><div className="mt-6 rounded-3xl border border-slate-800 bg-slate-900 p-7 sm:p-10"><p className="text-sm font-black text-blue-400">장군 AI 데이터 가이드</p><h1 className="mt-3 text-3xl font-black leading-tight">{a.title}</h1><p className="mt-5 leading-8 text-slate-300">{a.intro}</p><div className="mt-9 space-y-9">{a.sections.map(s=><section key={s.title}><h2 className="text-xl font-black">{s.title}</h2><p className="mt-3 leading-8 text-slate-300">{s.body}</p></section>)}</div><div className="mt-10 border-t border-slate-800 pt-6 text-sm leading-6 text-slate-500"><p>최종 검토: {a.updated} · 작성·검수: 장군 AI 편집팀</p><p>이 글은 공개된 야구 기록을 이해하기 위한 교육 자료이며 경기 결과나 금전적 이익을 보장하지 않습니다.</p></div></div></article></main>}
