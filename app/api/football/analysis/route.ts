import { NextRequest, NextResponse } from "next/server";

export const runtime = "edge";
export const dynamic = "force-dynamic";

const ESPN_CODES: Record<string,string>={epl:"eng.1",laliga:"esp.1",bundesliga:"ger.1",seriea:"ita.1",ucl:"uefa.champions",uel:"uefa.europa",uecl:"uefa.europa.conf",mls:"usa.1"};
const TEAM_KO:Record<string,string>={Arsenal:"아스널","Coventry City":"코번트리 시티","Manchester City":"맨체스터 시티","Manchester United":"맨체스터 유나이티드",Liverpool:"리버풀",Chelsea:"첼시","Tottenham Hotspur":"토트넘 홋스퍼",Girona:"지로나","Real Betis":"레알 베티스","Borussia Dortmund":"보루시아 도르트문트",Como:"코모","AS Monaco":"AS 모나코",Espanyol:"에스파뇰",Watford:"왓퍼드",Wrexham:"렉섬",Portsmouth:"포츠머스"};
Object.assign(TEAM_KO,{"Newcastle United":"뉴캐슬 유나이티드","Aston Villa":"애스턴 빌라","Brighton & Hove Albion":"브라이턴","West Ham United":"웨스트햄 유나이티드","Crystal Palace":"크리스털 팰리스",Everton:"에버턴",Fulham:"풀럼",Brentford:"브렌트퍼드","Nottingham Forest":"노팅엄 포리스트","Wolverhampton Wanderers":"울버햄프턴","Leeds United":"리즈 유나이티드",Sunderland:"선덜랜드",Burnley:"번리","AFC Bournemouth":"본머스","Real Madrid":"레알 마드리드",Barcelona:"바르셀로나","Atletico Madrid":"아틀레티코 마드리드","Atlético Madrid":"아틀레티코 마드리드","Athletic Club":"아틀레틱 빌바오",Villarreal:"비야레알","Real Sociedad":"레알 소시에다드",Sevilla:"세비야",Valencia:"발렌시아","Celta Vigo":"셀타 비고",Osasuna:"오사수나",Getafe:"헤타페",Mallorca:"마요르카","Rayo Vallecano":"라요 바예카노",Alavés:"알라베스","Bayern Munich":"바이에른 뮌헨","Bayer Leverkusen":"바이어 레버쿠젠","RB Leipzig":"RB 라이프치히","Eintracht Frankfurt":"프랑크푸르트","VfB Stuttgart":"슈투트가르트","SC Freiburg":"프라이부르크",Mainz:"마인츠","Werder Bremen":"베르더 브레멘","VfL Wolfsburg":"볼프스부르크","Union Berlin":"우니온 베를린",Internazionale:"인테르 밀란","Inter Milan":"인테르 밀란","AC Milan":"AC 밀란",Juventus:"유벤투스",Napoli:"나폴리","AS Roma":"AS 로마",Lazio:"라치오",Atalanta:"아탈란타",Fiorentina:"피오렌티나",Bologna:"볼로냐",Torino:"토리노",Genoa:"제노아",Udinese:"우디네세",Parma:"파르마",Cagliari:"칼리아리","Paris Saint-Germain":"파리 생제르맹","Blackburn Rovers":"블랙번 로버스","Sheffield Wednesday":"셰필드 웬즈데이","Hull City":"헐 시티","Derby County":"더비 카운티","Swansea City":"스완지 시티","Sheffield United":"셰필드 유나이티드","Birmingham City":"버밍엄 시티","Norwich City":"노리치 시티",Middlesbrough:"미들즈브러","Queens Park Rangers":"퀸스 파크 레인저스",Millwall:"밀월","Stoke City":"스토크 시티","Preston North End":"프레스턴 노스 엔드","Bristol City":"브리스톨 시티","Oxford United":"옥스퍼드 유나이티드"});
Object.assign(TEAM_KO,{Deportivo:"데포르티보 라코루냐","Deportivo La Coruña":"데포르티보 라코루냐","Deportivo La Coruna":"데포르티보 라코루냐","Genoa CFC":"제노아",Genoa:"제노아",Fiorentina:"피오렌티나","ACF Fiorentina":"피오렌티나","Las Palmas":"라스팔마스","Real Valladolid":"레알 바야돌리드","FC Andorra":"FC 안도라",Cádiz:"카디스",Cadiz:"카디스",Leganés:"레가네스",Leganes:"레가네스",Burgos:"부르고스","Burgos CF":"부르고스",Mirandés:"미란데스",Mirandes:"미란데스",Toulouse:"툴루즈","Toulouse FC":"툴루즈",Elche:"엘체","Real Oviedo":"레알 오비에도","Racing Santander":"라싱 산탄데르","Sporting Gijón":"스포르팅 히혼","Sporting Gijon":"스포르팅 히혼",Eibar:"에이바르",Almería:"알메리아",Almeria:"알메리아",Granada:"그라나다",Málaga:"말라가",Malaga:"말라가",Huesca:"우에스카",Zaragoza:"레알 사라고사",Córdoba:"코르도바",Cordoba:"코르도바",Castellón:"카스테욘",Castellon:"카스테욘",Albacete:"알바세테",Ceuta:"세우타"});
Object.assign(TEAM_KO,{"Atlanta United FC":"애틀랜타 유나이티드","Austin FC":"오스틴 FC","Charlotte FC":"샬럿 FC","Chicago Fire FC":"시카고 파이어","FC Cincinnati":"FC 신시내티","Colorado Rapids":"콜로라도 래피즈","Columbus Crew":"콜럼버스 크루","FC Dallas":"FC 댈러스","D.C. United":"DC 유나이티드","Houston Dynamo FC":"휴스턴 다이너모","Inter Miami CF":"인터 마이애미","LA Galaxy":"LA 갤럭시",LAFC:"로스앤젤레스 FC","Minnesota United FC":"미네소타 유나이티드","CF Montréal":"CF 몬트리올","Nashville SC":"내슈빌 SC","New England Revolution":"뉴잉글랜드 레볼루션","New York City FC":"뉴욕 시티 FC","Red Bull New York":"뉴욕 레드불스","Orlando City SC":"올랜도 시티","Philadelphia Union":"필라델피아 유니언","Portland Timbers":"포틀랜드 팀버스","Real Salt Lake":"레알 솔트레이크","San Diego FC":"샌디에이고 FC","San Jose Earthquakes":"산호세 어스퀘이크스","Seattle Sounders FC":"시애틀 사운더스","Sporting Kansas City":"스포팅 캔자스시티","St. Louis CITY SC":"세인트루이스 시티","Toronto FC":"토론토 FC","Vancouver Whitecaps":"밴쿠버 화이트캡스"});
const COMPETITION_KO:Record<string,string>={"Club Friendly":"클럽 친선경기","LALIGA 2":"스페인 2부리그","LaLiga 2":"스페인 2부리그",LALIGA:"스페인 라리가","Premier League":"잉글랜드 프리미어리그","EFL Championship":"잉글랜드 2부리그","UEFA Champions League":"유럽 챔피언스리그","Community Shield":"커뮤니티 실드","Emirates Cup":"에미레이츠컵"};
const competitionKo=(name:any)=>COMPETITION_KO[String(name??"")]??String(name??"");
const K_TEAM_KO:Record<string,string>={JEJU:"제주",ANYANG:"안양","FC ANYANG":"안양","DAEJEON HANA":"대전",DAEJEON:"대전",ULSAN:"울산",GANGWON:"강원",BUCHEON:"부천",GWANGJU:"광주","GWANG JU":"광주",INCHEON:"인천",POHANG:"포항",GIMCHEON:"김천","GIM CHEON":"김천",SEOUL:"서울","FC SEOUL":"서울",JEONBUK:"전북",DAEGU:"대구",SUWONFC:"수원FC"};
const kTeam=(name:any)=>K_TEAM_KO[String(name??"").toUpperCase()]??String(name??"팀");
const headers={"User-Agent":"Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 Chrome/140 Safari/537.36",Accept:"application/json,text/plain,*/*","Accept-Language":"ko-KR,ko;q=0.9,en;q=0.7"};

type Recent={id?:string;date:string;opponent:string;result:"승"|"무"|"패"|"";score:string;competition:string;homeAway?:"HOME"|"AWAY";homeTeam?:string;awayTeam?:string;homeScore?:string;awayScore?:string};
type Form={games:number;win:number;draw:number;loss:number;winRate:number;gf:number;ga:number;goalDifference:number;avgFor:number;avgAgainst:number;cleanSheetRate:number;scorelessRate:number;over15Rate:number;over25Rate:number;over35Rate:number;under25Rate:number;under35Rate:number;bttsRate:number};

async function fetchEspnSummary(code:string,event:string){
  const path=`site.api.espn.com/apis/site/v2/sports/soccer/${code}/summary?event=${event}`;
  const direct=await fetch(`https://${path}`,{headers,cache:"no-store"}).catch(()=>null);
  if(direct?.ok)return direct.json();
  const proxy=await fetch(`https://r.jina.ai/http://${path}`,{headers,cache:"no-store"});
  if(!proxy.ok)throw new Error(`상세 분석 데이터 조회 실패 (${proxy.status})`);
  const raw=await proxy.text(),start=raw.indexOf("{");
  if(start<0)throw new Error("상세 분석 응답을 해석하지 못했습니다.");
  return JSON.parse(raw.slice(start));
}

function eventToRecent(event:any,teamId:string,teamName:string):Recent{
  const at=event.atVs==="@"?"AWAY":"HOME";
  const opponent=TEAM_KO[event.opponent?.displayName]??event.opponent?.displayName??"상대팀",parts=String(event.score??"-").split("-").map((x:string)=>x.trim()),teamScore=parts[0]??"-",opponentScore=parts[1]??"-";
  const teamGoals=Number(teamScore),opponentGoals=Number(opponentScore),result:Recent["result"]=Number.isFinite(teamGoals)&&Number.isFinite(opponentGoals)?teamGoals>opponentGoals?"승":teamGoals<opponentGoals?"패":"무":event.gameResult==="W"?"승":event.gameResult==="L"?"패":"무";
  return {id:String(event.id??""),date:new Intl.DateTimeFormat("en-CA",{timeZone:"Asia/Seoul"}).format(new Date(event.gameDate)),opponent,result,score:String(event.score??"-"),competition:competitionKo(event.leagueAbbreviation??event.competitionName??""),homeAway:at,homeTeam:at==="HOME"?teamName:opponent,awayTeam:at==="HOME"?opponent:teamName,homeScore:at==="HOME"?teamScore:opponentScore,awayScore:at==="HOME"?opponentScore:teamScore};
}
function groupEvents(data:any,teamId:string):Recent[]{const group=(data.lastFiveGames??[]).find((x:any)=>String(x.team?.id)===teamId),teamName=TEAM_KO[group?.team?.displayName]??group?.team?.displayName??"팀";return (group?.events??[]).map((e:any)=>eventToRecent(e,teamId,teamName));}
async function recentTen(data:any,code:string,teamId:string,before:string){
  const initial=groupEvents(data,teamId).filter(x=>x.date<before);
  const oldest=initial.at(0);
  let extra:Recent[]=[];
  if(oldest?.id){try{extra=groupEvents(await fetchEspnSummary(code,oldest.id),teamId).filter(x=>x.date<oldest.date)}catch{/* 확보된 경기만 사용 */}}
  return Array.from(new Map([...initial,...extra].map(x=>[x.id||`${x.date}|${x.opponent}`,x])).values()).filter(x=>x.date<before).sort((a,b)=>b.date.localeCompare(a.date)).slice(0,10);
}
function pct(n:number,d:number){return d?Math.round(n/d*100):0}
function form(games:Recent[]):Form{let win=0,draw=0,loss=0,gf=0,ga=0,cs=0,blank=0,o15=0,o25=0,o35=0,btts=0;for(const g of games){if(g.result==="승")win++;else if(g.result==="패")loss++;else draw++;const [a,b]=g.score.split("-").map(Number);if(!Number.isFinite(a)||!Number.isFinite(b))continue;gf+=a;ga+=b;if(b===0)cs++;if(a===0)blank++;if(a+b>1.5)o15++;if(a+b>2.5)o25++;if(a+b>3.5)o35++;if(a>0&&b>0)btts++}const n=games.length;return{games:n,win,draw,loss,winRate:pct(win,n),gf,ga,goalDifference:gf-ga,avgFor:n?Number((gf/n).toFixed(2)):0,avgAgainst:n?Number((ga/n).toFixed(2)):0,cleanSheetRate:pct(cs,n),scorelessRate:pct(blank,n),over15Rate:pct(o15,n),over25Rate:pct(o25,n),over35Rate:pct(o35,n),under25Rate:100-pct(o25,n),under35Rate:100-pct(o35,n),bttsRate:pct(btts,n)} }
function formIndex(games:Recent[]){if(!games.length)return null;let earned=0,max=0;games.forEach((g,i)=>{const w=10-i*.65;earned+=(g.result==="승"?3:g.result==="무"?1:0)*w;max+=3*w});const score=Math.round(earned/max*100);return{score,label:score>=85?"매우 좋음":score>=70?"좋음":score>=50?"보통":score>=30?"나쁨":"매우 나쁨"}}
function venueForm(games:Recent[],venue:"HOME"|"AWAY"){return form(games.filter(x=>x.homeAway===venue).slice(0,10))}
function fatigue(games:Recent[],matchDate:string){if(!games.length)return null;const target=new Date(`${matchDate}T12:00:00Z`).getTime(),last=new Date(`${games[0].date}T12:00:00Z`).getTime();const days=Math.max(0,Math.round((target-last)/86400000));const inDays=(n:number)=>games.filter(g=>target-new Date(`${g.date}T12:00:00Z`).getTime()<=n*86400000).length;const seven=inDays(7),fourteen=inDays(14);return{lastDate:games[0].date,restDays:days,last7:seven,last14:fourteen,level:days<=3||seven>=3?"높음":days<=5||fourteen>=4?"보통":"낮음"}}
function h2hStats(games:Recent[],home:string,away:string){let hw=0,d=0,aw=0,total=0,o25=0,btts=0;for(const g of games){const [a,b]=g.score.split("-").map(Number);if(!Number.isFinite(a)||!Number.isFinite(b))continue;if(a>b)hw++;else if(a<b)aw++;else d++;total+=a+b;if(a+b>2.5)o25++;if(a>0&&b>0)btts++}const n=games.length;return{games:n,home,away,homeWins:hw,draws:d,awayWins:aw,avgTotal:n?Number((total/n).toFixed(2)):0,over25Rate:pct(o25,n),under25Rate:100-pct(o25,n),bttsRate:pct(btts,n)}}
function findEntries(node:any,out:any[]=[]):any[]{if(!node||typeof node!=="object")return out;if(Array.isArray(node.entries))out.push(...node.entries);for(const v of Object.values(node))if(v&&typeof v==="object")findEntries(v,out);return out}
function standing(data:any,id:string){const e=findEntries(data).find((x:any)=>String(x.team?.id??x.id)===id);const s=Object.fromEntries((e?.stats??[]).map((x:any)=>[x.name,x.value??x.displayValue]));return e?{rank:Number(s.rank??e.position)||null,points:Number(s.points??0),played:Number(s.gamesPlayed??0),wins:Number(s.wins??0),draws:Number(s.ties??0),losses:Number(s.losses??0),goalDifference:Number(s.pointDifferential??0)}:null}
function american(v:any){const n=Number(v);if(!Number.isFinite(n)||n===0)return null;return n<0?(-n)/(-n+100):100/(n+100)}
function marketFrom(odds:any){if(!odds)return null;const raw=[american(odds.homeTeamOdds?.moneyLine),american(odds.drawOdds?.moneyLine),american(odds.awayTeamOdds?.moneyLine)];const sum=raw.reduce((a:number,b:number|null)=>a+(b??0),0);const probabilities=raw.every(Boolean)&&sum?raw.map(x=>Math.round((x!/sum)*100)):null;if(probabilities){probabilities[1]+=100-probabilities.reduce((a,b)=>a+b,0)}return{details:odds.details??null,spread:odds.spread??null,overUnder:odds.overUnder??null,overOdds:odds.overOdds??null,underOdds:odds.underOdds??null,provider:odds.provider?.name??null,moneyLine:{home:odds.homeTeamOdds?.moneyLine??null,draw:odds.drawOdds?.moneyLine??null,away:odds.awayTeamOdds?.moneyLine??null},probabilities}}
function poisson(lambda:number,k:number){let p=Math.exp(-lambda);for(let i=1;i<=k;i++)p*=lambda/i;return p}
function model(homeForm:Form,awayForm:Form,homeVenue:Form,awayVenue:Form,homeStanding:any,awayStanding:any,market:any,reliability:string){
  const hv=homeVenue.games>=3?homeVenue:homeForm,av=awayVenue.games>=3?awayVenue:awayForm;
  let hx=Math.max(.2,Math.min(4,(hv.avgFor+av.avgAgainst)/2*1.08)),ax=Math.max(.2,Math.min(4,(av.avgFor+hv.avgAgainst)/2*.92));
  if(homeStanding?.rank&&awayStanding?.rank){const adj=Math.max(-.3,Math.min(.3,(awayStanding.rank-homeStanding.rank)*.018));hx+=adj;ax-=adj}
  const marketProb=market?.probabilities;
  if(marketProb){const strengthGap=(marketProb[0]-marketProb[2])/100,goalAdjustment=Math.max(-.65,Math.min(.65,strengthGap*.8));hx=Math.max(.2,hx+goalAdjustment);ax=Math.max(.2,ax-goalAdjustment)}
  const scores:{score:string;p:number}[]=[];let hp=0,dp=0,ap=0;for(let h=0;h<=7;h++)for(let a=0;a<=7;a++){const p=poisson(hx,h)*poisson(ax,a);scores.push({score:`${h}-${a}`,p});if(h>a)hp+=p;else if(h<a)ap+=p;else dp+=p}
  if(reliability==="C"||reliability==="D"){hp=hp*.8+.2/3;dp=dp*.8+.2/3;ap=ap*.8+.2/3}
  if(marketProb){const blend=.55;hp=hp*(1-blend)+(marketProb[0]/100)*blend;dp=dp*(1-blend)+(marketProb[1]/100)*blend;ap=ap*(1-blend)+(marketProb[2]/100)*blend}
  const total=hp+dp+ap,prob=[Math.round(hp/total*100),Math.round(dp/total*100),Math.round(ap/total*100)];prob[1]+=100-prob.reduce((a,b)=>a+b,0);
  const sortedScores=scores.sort((a,b)=>b.p-a.p);
  const top=sortedScores.slice(0,5).map(x=>({score:x.score,probability:Math.round(x.p*100)}));
  const outcomeIndex=prob.indexOf(Math.max(...prob));
  const matchesOutcome=(score:string)=>{const [h,a]=score.split("-").map(Number);return outcomeIndex===0?h>a:outcomeIndex===1?h===a:h<a};
  const totalGoalsMean=hx+ax;
  const chanceAtLeast=(goals:number)=>Math.round((1-Array.from({length:goals},(_,k)=>poisson(totalGoalsMean,k)).reduce((sum,value)=>sum+value,0))*100);
  const over15=chanceAtLeast(2),over25=chanceAtLeast(3),over35=chanceAtLeast(4);
  const btts=Math.round((1-Math.exp(-hx)-Math.exp(-ax)+Math.exp(-(hx+ax)))*100);
  const targetTotal=over35>=50?Math.max(4,Math.round(totalGoalsMean)):over25>=50?3:over15>=50?2:totalGoalsMean>=.7?1:0;
  const outcomeScores=sortedScores.filter(x=>matchesOutcome(x.score));
  const bttsPreference=btts>=50;
  const styleScores=outcomeIndex===1?outcomeScores:outcomeScores.filter(x=>{const [h,a]=x.score.split("-").map(Number);return (h>0&&a>0)===bttsPreference});
  const representativePool=styleScores.length?styleScores:outcomeScores;
  const representativeScore=representativePool.sort((x,y)=>{const xt=x.score.split("-").map(Number).reduce((a,b)=>a+b,0),yt=y.score.split("-").map(Number).reduce((a,b)=>a+b,0);return Math.abs(xt-targetTotal)-Math.abs(yt-targetTotal)||y.p-x.p})[0]?.score??top[0]?.score??"-";
  const marketHome=market?.probabilities?.[0]??null;
  return{expectedGoals:{home:Number(hx.toFixed(2)),away:Number(ax.toFixed(2))},probabilities:{home:prob[0],draw:prob[1],away:prob[2]},topScores:top,representativeScore,representativeOutcome:outcomeIndex===0?"home":outcomeIndex===1?"draw":"away",over15,over25,over35,under25:100-over25,btts,marketHome,difference:marketHome==null?null:prob[0]-marketHome};
}
function power(formData:Form,index:any,venue:Form,standingData:any){const parts:{v:number;w:number}[]=[];if(index)parts.push({v:index.score,w:20});if(formData.games)parts.push({v:Math.max(0,Math.min(100,50+(formData.avgFor-formData.avgAgainst)*14+(formData.winRate-50)*.35)),w:30});if(venue.games)parts.push({v:Math.max(0,Math.min(100,50+(venue.avgFor-venue.avgAgainst)*16+(venue.winRate-50)*.3)),w:25});if(standingData?.rank)parts.push({v:Math.max(15,100-standingData.rank*4),w:25});const w=parts.reduce((a,b)=>a+b.w,0);return w?Math.round(parts.reduce((a,b)=>a+b.v*b.w,0)/w):50}

function buildAnalysis(base:any){const {home,away,homeRecent,awayRecent,h2h,homeStanding,awayStanding,market,date}=base;const homeForm=form(homeRecent),awayForm=form(awayRecent),homeIndex=formIndex(homeRecent),awayIndex=formIndex(awayRecent),homeVenue=venueForm(homeRecent,"HOME"),awayVenue=venueForm(awayRecent,"AWAY");const available=[homeRecent.length>=8,awayRecent.length>=8,!!homeStanding,!!awayStanding,!!market, h2h.length>=3];const count=available.filter(Boolean).length,reliability=count>=5?"A":count>=3?"B":count>=2?"C":"D";const prediction=model(homeForm,awayForm,homeVenue,awayVenue,homeStanding,awayStanding,market,reliability);const homePower=power(homeForm,homeIndex,homeVenue,homeStanding),awayPower=power(awayForm,awayIndex,awayVenue,awayStanding);const lean=prediction.probabilities.home>prediction.probabilities.away+8?`${home} 우세`:prediction.probabilities.away>prediction.probabilities.home+8?`${away} 우세`:"접전";const keyPoints=[`${home} 최근 ${homeForm.games}경기 ${homeForm.win}승 ${homeForm.draw}무 ${homeForm.loss}패`,`${away} 최근 ${awayForm.games}경기 ${awayForm.win}승 ${awayForm.draw}무 ${awayForm.loss}패`,homeVenue.games?`${home} 최근 홈 경기 평균 ${homeVenue.avgFor}득점·${homeVenue.avgAgainst}실점`:null,awayVenue.games?`${away} 최근 원정 경기 평균 ${awayVenue.avgFor}득점·${awayVenue.avgAgainst}실점`:null,`양 팀 최근 경기에서 두 팀 합계 3골 이상이 나온 비율 ${Math.round((homeForm.over25Rate+awayForm.over25Rate)/2)}%`].filter(Boolean).slice(0,5);const risks=[h2h.length<3?"두 팀의 최근 직접 맞대결 표본이 적어 상대전적의 예측 비중을 낮췄습니다.":null,!homeStanding||!awayStanding?"현재 시즌 순위 데이터가 완전하지 않습니다.":null,!market?"시장 배당 데이터가 제공되지 않았습니다.":null,homeRecent.length<10||awayRecent.length<10?"최근 경기 데이터가 10경기보다 적은 팀이 있습니다.":null].filter(Boolean);const narrative=[`${home}은 최근 ${homeForm.games}경기에서 ${homeForm.win}승을 기록했고 경기당 ${homeForm.avgFor}득점, ${homeForm.avgAgainst}실점 흐름입니다.`,`${away}은 최근 ${awayForm.games}경기에서 ${awayForm.win}승을 기록했고 경기당 ${awayForm.avgFor}득점, ${awayForm.avgAgainst}실점입니다.`,homeVenue.games&&awayVenue.games?`홈·원정 표본에서는 ${home}의 홈 득점 ${homeVenue.avgFor}와 ${away}의 원정 실점 ${awayVenue.avgAgainst}이 핵심 매치업입니다.`:"홈·원정 표본이 충분하지 않아 전체 최근 흐름의 비중을 높였습니다.",`모델은 실제 득실점과 홈·원정 성적을 반영해 ${prediction.expectedGoals.home} 대 ${prediction.expectedGoals.away}의 모델 예상 득점을 계산했습니다.`,market?.probabilities?`해외 배당의 홈팀 승리 예상은 ${market.probabilities[0]}%, 사이트 분석의 홈팀 승리 예상은 ${prediction.probabilities.home}%입니다.`:"시장 확률 데이터가 없어 자체 모델 결과만 제시합니다.",risks[0]??"최근 경기와 시즌 전력을 함께 반영했지만 당일 라인업 변수는 별도 확인이 필요합니다.",`종합적으로 ${lean} 구도입니다. 승패 확률, 총득점 가능성, 양 팀 득점 가능성을 함께 반영한 대표 예상 스코어는 ${prediction.representativeScore}입니다.`];return{homeForm,awayForm,homeIndex,awayIndex,homeVenue,awayVenue,homeFatigue:fatigue(homeRecent,date),awayFatigue:fatigue(awayRecent,date),h2hStats:h2hStats(h2h,home,away),prediction,power:{home:homePower,away:awayPower,difference:homePower-awayPower},lean,keyPoints,risks,reliability,narrative,attackAdvantage:homeForm.avgFor>awayForm.avgFor?home:awayForm.avgFor>homeForm.avgFor?away:"대등",defenseAdvantage:homeForm.avgAgainst<awayForm.avgAgainst?home:awayForm.avgAgainst<homeForm.avgAgainst?away:"대등"}}

async function espn(params:URLSearchParams){const league=params.get("league")??"",code=ESPN_CODES[league],gameId=params.get("gameId")??"",date=params.get("date")??"";if(!code||!gameId)throw new Error("유럽 축구 경기 식별자가 없습니다.");const data=await fetchEspnSummary(code,gameId),comp=data.header?.competitions?.[0]??{},hc=comp.competitors?.find((x:any)=>x.homeAway==="home"),ac=comp.competitors?.find((x:any)=>x.homeAway==="away");const home=params.get("home")??TEAM_KO[hc?.team?.displayName]??hc?.team?.displayName??"홈팀",away=params.get("away")??TEAM_KO[ac?.team?.displayName]??ac?.team?.displayName??"원정팀",homeId=params.get("homeId")??String(hc?.team?.id??""),awayId=params.get("awayId")??String(ac?.team?.id??"");const [homeRecent,awayRecent]=await Promise.all([recentTen(data,code,homeId,date),recentTen(data,code,awayId,date)]);const h2h=(data.seasonseries??[]).flatMap((s:any)=>(s.events??[]).map((e:any)=>({id:String(e.id??""),date:e.date?new Intl.DateTimeFormat("en-CA",{timeZone:"Asia/Seoul"}).format(new Date(e.date)):"",opponent:`${home} vs ${away}`,result:"",score:e.competitors?.map((c:any)=>c.score?.displayValue??c.score).join("-")??"-",competition:s.title??"상대전적"}))).sort((a:any,b:any)=>b.date.localeCompare(a.date)).slice(0,10);const homeStanding=standing(data.standings,homeId),awayStanding=standing(data.standings,awayId),market=marketFrom((data.pickcenter??data.odds??[])[0]);const base={home,away,homeId,awayId,date,homeRecent,awayRecent,h2h,homeStanding,awayStanding,market,source:"ESPN 경기 상세·리그 순위·배당"};return{...base,...buildAnalysis(base)}}

async function kMonth(year:string,month:string){const r=await fetch("https://www.kleague.com/getScheduleList.do",{method:"POST",headers:{"content-type":"application/json; charset=utf-8",accept:"application/json","user-agent":"Janggun-AI/1.0"},body:JSON.stringify({year,month,leagueId:1}),cache:"no-store"});if(!r.ok)throw new Error(`K리그 일정 조회 실패 (${r.status})`);const d=await r.json();return d.data??d}
function kRecent(games:any[],id:string,before:string){return games.filter(g=>g.endYn==="Y"&&g.gameDate<before&&(g.homeTeam===id||g.awayTeam===id)).sort((a,b)=>b.gameDate.localeCompare(a.gameDate)||String(b.gameTime).localeCompare(String(a.gameTime))).slice(0,10).map(g=>{const isHome=g.homeTeam===id,gf=Number(isHome?g.homeGoal:g.awayGoal),ga=Number(isHome?g.awayGoal:g.homeGoal),homeName=kTeam(g.homeTeamName),awayName=kTeam(g.awayTeamName);return{id:String(g.gameId??g.scheduleId??""),date:g.gameDate.replaceAll(".","-"),opponent:isHome?awayName:homeName,result:gf>ga?"승":gf<ga?"패":"무",score:`${gf}-${ga}`,competition:g.meetName??"K리그1",homeAway:isHome?"HOME":"AWAY",homeTeam:homeName,awayTeam:awayName,homeScore:String(g.homeGoal),awayScore:String(g.awayGoal)} as Recent})}
async function kleague(params:URLSearchParams){const date=params.get("date")??"",home=params.get("home")??"홈팀",away=params.get("away")??"원정팀",homeId=params.get("homeId")??"",awayId=params.get("awayId")??"",baseDate=new Date(`${date}T00:00:00+09:00`);const months=Array.from({length:12},(_,i)=>{const d=new Date(baseDate);d.setMonth(d.getMonth()-i);return[String(d.getFullYear()),String(d.getMonth()+1).padStart(2,"0")]});const payloads=await Promise.all(months.map(([y,m])=>kMonth(y,m))),raw=payloads.flatMap(x=>x.scheduleList??[]),games=Array.from(new Map(raw.map((g:any)=>[String(g.gameId??g.scheduleId??`${g.gameDate}|${g.gameTime}|${g.homeTeam}|${g.awayTeam}`),g])).values()) as any[],before=date.replaceAll("-","."),homeRecent=kRecent(games,homeId,before),awayRecent=kRecent(games,awayId,before),h2h=games.filter(g=>g.endYn==="Y"&&g.gameDate<before&&((g.homeTeam===homeId&&g.awayTeam===awayId)||(g.homeTeam===awayId&&g.awayTeam===homeId))).sort((a,b)=>b.gameDate.localeCompare(a.gameDate)).slice(0,10).map(g=>({id:String(g.gameId??""),date:g.gameDate.replaceAll(".","-"),opponent:`${home} vs ${away}`,result:"",score:`${g.homeGoal}-${g.awayGoal}`,competition:g.meetName??"K리그1"} as Recent));const rr=await fetch(`https://www.kleague.com/record/teamRank.do?leagueId=1&year=${date.slice(0,4)}&stadium=all&recordType=rank`,{method:"POST",headers,cache:"no-store"}),rd=rr.ok?await rr.json():{},rows=rd.data?.teamRank??rd.teamRank??[],get=(id:string)=>{const x=rows.find((r:any)=>r.teamId===id);return x?{rank:Number(x.rank),points:Number(x.gainPoint),played:Number(x.gameCount),wins:Number(x.winCnt),draws:Number(x.tieCnt),losses:Number(x.lossCnt),goalDifference:Number(x.gapCnt)}:null},homeStanding=get(homeId),awayStanding=get(awayId),base={home,away,homeId,awayId,date,homeRecent,awayRecent,h2h,homeStanding,awayStanding,market:null,source:"K리그 공식 일정·순위"};return{...base,...buildAnalysis(base)}}

export async function GET(req:NextRequest){try{const data=req.nextUrl.searchParams.get("league")==="kleague"?await kleague(req.nextUrl.searchParams):await espn(req.nextUrl.searchParams);return NextResponse.json({success:true,...data,updatedAt:new Date().toISOString()},{headers:{"Cache-Control":"public, s-maxage=600, stale-while-revalidate=1200"}})}catch(e){return NextResponse.json({success:false,message:e instanceof Error?e.message:"축구 분석을 불러오지 못했습니다."},{status:502})}}
